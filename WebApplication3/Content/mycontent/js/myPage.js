function goPage(pno, psize) {	var itable = document.getElementById("idData");	var num = itable.rows.length;	console.log(num);	var totalPage = 0;	var pageSize = psize;	if (num / pageSize > parseInt(num / pageSize)) {		totalPage = parseInt(num / pageSize) + 1;	} else {		totalPage = parseInt(num / pageSize);	}	var currentPage = pno;	var startRow = (currentPage - 1) * pageSize + 1;	var endRow = currentPage * pageSize;	endRow = (endRow > num) ? num : endRow; 40	console.log(endRow);	for (var i = 1; i < (num + 1); i++) {		var irow = itable.rows[i - 1];		if (i >= startRow && i <= endRow) {			irow.style.display = "block";		} else {			irow.style.display = "none";		}	}	var pageEnd = document.getElementById("pageEnd");	var tempStr = "共" + num + "条记录 分" + totalPage + "页 当前第" + currentPage + "页 ";	if (currentPage > 1) {		tempStr += "<a href=\"#\" onClick=\"goPage(" + (1) + "," + psize + ")\">首页</a>";		tempStr += "<a href=\"#\" onClick=\"goPage(" + (currentPage - 1) + "," + psize + ")\"><上一页</a>"	} else {		tempStr += "首页";		tempStr += "<上一页";	}	if (currentPage < totalPage) {		tempStr += "<a href=\"#\" onClick=\"goPage(" + (currentPage + 1) + "," + psize + ")\">下一页></a>";		tempStr += "<a href=\"#\" onClick=\"goPage(" + (totalPage) + "," + psize + ")\">尾页</a>";	} else {		tempStr += "下一页>";		tempStr += "尾页";	}	tempStr += "<input type='text' id='qw'><input type='button' id='btn' value='跳转' onclick='fun()'>";	document.getElementById("barcon").innerHTML = tempStr;}function fun() {	var itable = document.getElementById("idData");	var num = itable.rows.length;	var totalPage = 0;	if (num / 9 > parseInt(num / 9)) {		totalPage = parseInt(num / 9) + 1;	} else {		totalPage = parseInt(num / 9);	}	if (($("#qw").val()) >= 1 && ($("#qw").val()) <= totalPage) {		goPage(($("#qw").val()), 9);	}}